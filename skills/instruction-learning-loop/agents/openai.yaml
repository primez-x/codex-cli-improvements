interface:
  display_name: "Instruction Learning Loop"
  short_description: "Apply corrections as lean, validated instructions"
  default_prompt: "Use $instruction-learning-loop when a source-backed instruction correction is warranted; propose or apply the smallest authorized change, then run the explicit audit."
